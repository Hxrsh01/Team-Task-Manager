# Server
PORT=5000
NODE_ENV=production

# PostgreSQL — use the Railway-provided DATABASE_URL from the Postgres plugin
DATABASE_URL=postgresql://user:password@host:5432/dbname

# JWT
JWT_SECRET=your_strong_random_secret_here

# Frontend URL (set to your Railway frontend service URL)
FRONTEND_URL=https://your-frontend.up.railway.app
